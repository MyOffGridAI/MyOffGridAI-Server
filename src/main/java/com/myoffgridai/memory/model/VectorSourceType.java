package com.myoffgridai.memory.model;

/**
 * Identifies the source type of a vector embedding stored in the vector document table.
 */
public enum VectorSourceType {
    MEMORY,
    CONVERSATION,
    KNOWLEDGE_CHUNK
}
